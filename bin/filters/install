cat >> .git/config <<EOF
[filter "$1"]
    smudge = git filter $1 smudge
    clean = git filter $1 clean
[diff "$1"]
    textconv = git filter $1 textconv
EOF

cat > .gitattributes <<EOF
* filter=$1 diff=$1
EOF
